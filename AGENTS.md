# Dragon Sky Race Project Rules

## Purpose

이 저장소는 브라우저용 드래곤 공중 레이싱 게임 **하늘매듭배**를 만든다. 작업자는 코드보다 플레이 감각, 코스 가독성, 실제 브라우저 검증을 우선한다.

## Sources Of Truth

작업 전 다음 순서로 읽는다.

1. 최신 사용자 지시
2. 이 `AGENTS.md`
3. `docs/PRODUCT_GOAL.md`
4. `docs/IMPLEMENTATION_PLAN.md`
5. `.codex/skills/build-dragon-sky-race/SKILL.md`

서로 충돌하면 위 순서가 우선한다. 범위나 제품 결정이 바뀌면 코드보다 제품 목표와 계획을 먼저 갱신한다.

## Planning Gate

- `docs/PRODUCT_GOAL.md`가 `초안 / 구현 게이트 닫힘`인 동안 `src/`, `package.json`, 런타임 에셋을 만들거나 의존성을 설치하지 않는다.
- 사용자가 계획을 승인하면 제품 목표 상태를 `승인됨 / 구현 게이트 열림`으로 변경한 뒤 Milestone 0을 시작한다.
- 구현 중 새 기능이 첫 릴리스 범위를 바꾸면 멈추고 계획 문서에 먼저 기록한다.

## Product Invariants

- 첫 릴리스는 싱글 플레이, 동일 능력치의 비행 생명체 3종, 코스 1개, 10~12개 관문의 1랩 타임 트라이얼이다.
- 비행은 자동 전진형 아케이드 모델이다. 정지, 후진, 사실적 강체 비행을 추가하지 않는다.
- 키보드와 터치 모두 시작부터 완주와 재시도까지 지원한다.
- 다음 관문만 유효하며 순서대로 모두 통과해야 완주한다.
- 리스폰은 마지막 통과 관문의 진행 상태를 보존하고 타이머를 계속 진행한다.
- 멀티플레이, 백엔드, 계정, 서버 순위표, 전투, 퀘스트와 능력치·유료 재화형 커스터마이징은 범위 밖이다.
- RC11 커스터마이징은 형태 3종, 팔레트 3종, 장식 3종의 외형 전용 수직 슬라이스이며 비행·충돌·기록 규칙을 바꾸지 않는다.

## Dragon And World Rules

- 드래곤 동작은 날갯짓, 어깨 선행 뱅크, 몸통/꼬리 지연, 상승/부스트 자세로 표현한다.
- 자동차식 바퀴, 배기, 지상 속도계, 지상 트랙 문법을 사용하지 않는다.
- 기본 핵심 시각 신호는 주홍 드래곤, 두 가닥 바람실, 금빛 관문이다. 다른 캐릭터도 바람실과 관문보다 먼저 시선을 빼앗지 않는다.
- Three.js 장면은 full-bleed이며 장식 카드나 프레임 안에 넣지 않는다.
- 다음 관문은 항상 읽혀야 하며 풍경과 HUD가 비행로를 가려서는 안 된다.
- 참고 사이트의 코드, 에셋, 브랜딩, 레이아웃을 복제하지 않는다.

## Engineering Rules

- Vite vanilla TypeScript와 Three.js를 기본 스택으로 사용한다. React나 상태 관리 라이브러리를 추가하지 않는다.
- 게임 규칙과 렌더 오브젝트를 분리한다. 관문, 레이스 상태, 비행, 저장 로직은 가능한 한 순수 함수로 유지한다.
- 시뮬레이션은 60Hz 고정 스텝과 제한된 델타를 사용한다.
- 고속 관문 판정은 이전/현재 위치 선분을 사용하며 현재 위치 한 점만 검사하지 않는다.
- 반복 풍경은 인스턴싱을 우선하고 DPR, 그림자, 입자 예산을 명시한다.
- 추상화는 실제 중복이나 경계를 줄일 때만 추가한다. 작은 프로젝트를 프레임워크처럼 만들지 않는다.
- 사용 에셋의 출처와 라이선스를 기록한다. 출처가 불명확한 파일은 넣지 않는다.

## Test-First Rules

- 레이스 상태, 관문 교차, 부스트, 일시정지, 저장 파싱은 실패하는 테스트를 먼저 작성한다.
- 각 마일스톤은 `test`, `typecheck`, `lint`, `build`가 통과한 상태로 끝낸다.
- 기능이 브라우저에 보이면 실제 브라우저에서 데스크톱과 모바일을 모두 확인한다.
- WebGL 캔버스는 스크린샷만 보지 말고 비어 있지 않은 픽셀과 시간에 따른 픽셀 변화를 검사한다.
- 시각 변경은 매 반복마다 실제 브라우저 스크린샷과 캔버스 픽셀 검사로 오버랩, 프레이밍, 가독성을 판정한다.
- 콘솔 오류와 처리되지 않은 Promise rejection이 있으면 완료로 보고하지 않는다.

## UI Rules

- 게임 화면에 사용법을 장황하게 설명하지 않는다. 시작 화면에는 현재 입력 장치에 필요한 핵심 조작만 보여 준다.
- 익숙한 명령은 아이콘 버튼을 사용하고 모든 아이콘에 `aria-label`과 툴팁을 제공한다.
- 터치 대상은 최소 44x44 CSS px이며 safe-area를 반영한다.
- 타이머는 tabular numerals와 안정된 너비를 사용해 숫자가 바뀌어도 레이아웃이 움직이지 않게 한다.
- `prefers-reduced-motion`에서 카메라 흔들림, FOV 펄스, 강한 속도선을 줄인다.
- `1440x900`, `1280x720`, `844x390`, `390x844`, `320x568`을 최소 검증 뷰포트로 사용한다.

## Delivery Rules

- 한 번에 하나의 마일스톤 또는 하나의 수직 슬라이스만 구현한다.
- 구현 전 수정 파일과 완료 조건을 짧게 밝힌다.
- 구현 후 변경 파일, 단순화한 부분, 실행한 검증, 남은 위험을 보고한다.
- 빌드 결과물, `node_modules`, 임시 스크린샷, 디버그 로그를 커밋하지 않는다.
- 커밋을 요청받으면 루트 Lore Commit Protocol을 따른다.
