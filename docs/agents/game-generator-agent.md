# Game Generator Agent

Read `AGENT.md`, `VERIFY.md`, `CODEX_GOAL.md`, `docs/harness/contract.md`, and the latest feedback before editing. Implement only the current round. Preserve the existing game rules and avoid unrelated refactors. Run all deterministic gates, capture browser/render evidence for visible work when possible, and write the required handoff with exact evidence. Never claim human validation or push to the remote.
